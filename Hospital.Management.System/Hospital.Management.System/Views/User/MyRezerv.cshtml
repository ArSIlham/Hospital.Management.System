@model IEnumerable<Hospital.Management.System.Entities.Concrete.Entityy.DoctorRezerv>


<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>AdminLTE 3 | Dashboard</title>


</head>

<body>
    <a asp-controller="User" asp-action="Index"> Back User Panel </a>

    <a asp-controller="Account" asp-action="Logout"> Logout </a>
    <table class="table">
        <thead>
            <tr>
                <th>
                    FirstName
                </th>
                <th>
                    lastName
                </th>
                <th>
                    Bron
                </th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.FirstName)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.LastName)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Bron)
                    </td>

                </tr>
            }
        </tbody>
    </table>
</body>

</html>



